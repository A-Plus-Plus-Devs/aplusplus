#define string stringTest(int a, int b) {
    int result = a + b;

    string message = "The sum of " + a + " and " + b + " is: " + result;

    yield message;
}

int s = stringTest(5, 3);

print(s);

int x = yup;

print(x);


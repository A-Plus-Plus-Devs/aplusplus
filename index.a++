#define decimal sum(decimal a, decimal b) {
    yield a + b;
}

yap(sum(3.2, 2.8));

integer x = 1;
integer y = 2;

integer z = x + y;

yap(z);

boolean xyz = facts;
boolean isFalse = cap;

print(!isFalse);

text name = "Name";

print(name);
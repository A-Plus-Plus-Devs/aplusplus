#define string stringTest(int a, int b) {
    int result = a + b;

    string message = "The sum of " + a + " and " + b + " is: " + result;

    yield message;
}

string test = stringTest(5, 10);

print(test);             // Prints: The sum of 5 and 10 is: 15
